export const NUM_ROWS = 38;
export const NUM_COLS = 85;
export const START_NODE_ROW = Math.floor(NUM_ROWS / 2);
export const START_NODE_COL = Math.floor(NUM_COLS / 4);
export const FINISH_NODE_ROW = Math.floor(NUM_ROWS / 2);
export const FINISH_NODE_COL = Math.floor((NUM_COLS * 3) / 4);
